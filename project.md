# 项目计划：经济政策智能分析系统 (EcoPolicy AI)

## 一、项目目标

构建一个**通用化的经济政策智能分析平台**，为任意企业/行业/区域提供：

- 政策文本的自动抓取与结构化解析
- 企业画像与政策的多维智能匹配
- 行动导向的决策建议与报告生成

核心价值：**企业画像 x 政策文本 = 结构化决策建议**

---

## 二、系统范围

| 子系统 | 功能 | 入口 |
|:--|:--|:--|
| **政策监控** | 多源政策抓取、解析、关键词匹配、通知 | `policy_monitor/main.py` |
| **Agent 编排** | 企业匹配、简报生成、工作流调度 | `agent/agent.py` |
| **配置框架** | 企业画像模板、行业矩阵、输出规范 | `config/` |

---

## 三、技术架构

- **语言**：Python 3.12+
- **存储**：SQLite（本地数据库）
- **网络**：requests + BeautifulSoup4（安全爬取）
- **配置**：YAML（可读性强，易于扩展）
- **AI 集成**：CLAUDE.md 指令文件 + agent_system_prompt.md

### 数据流

```
政策源（API/RSS/HTML）
  → fetcher.py（安全请求）
    → parsers/（结构化解析）
      → database.py（SQLite 存储）
        → matcher.py（关键词匹配）
          → enterprise_matcher.py（企业匹配）
            → report_generator.py（简报生成）
              → notifier.py（通知推送）
```

---

## 四、里程碑

### Phase 1：基础系统（已完成）

- [x] 政策监控工具核心（fetcher + parsers + database）
- [x] 关键词匹配器 + P0/P1/P2 评分
- [x] 25 个国家/省/市级数据源接入
- [x] 产业分类体系（5 大类 43 子行业）
- [x] 省市级区域配置加载器
- [x] Agent 编排系统（scanner + matcher + reporter + notifier）
- [x] 企业画像模板（10 大维度）
- [x] 六步标准工作流
- [x] 输出规范（5/5 分制、P0/P1/P2、商务简报风格）

### Phase 2：验证与优化（当前）

- [x] 多行业案例验证（种业 + 制造业 + 数字经济 + 新能源 + 生物医药）
- [ ] 反馈闭环机制（追踪申报结果 vs 建议）
- [ ] 定时调度集成（Windows Task Scheduler / cron）

### Phase 3：社区化

- [ ] 更多行业分析模板
- [ ] 更多省市数据源
- [ ] Web UI（可选）
- [ ] 接入更多 AI 模型

---

## 五、工作方法

- **配置驱动**：所有扩展通过 YAML 配置完成，无需修改代码
- **渐进式扩展**：从单一企业/政策起步，逐步增加行业和区域
- **安全优先**：robots.txt 遵守、限速抓取、零法律风险
- **模块解耦**：policy_monitor 和 agent 可独立使用

---

## 六、约束

| 类型 | 约束 |
|:--|:--|
| **技术** | 纯 Python，依赖最小化；SQLite 本地存储 |
| **法律** | 所有数据源为公开信息；遵守 robots.txt |
| **隐私** | 企业画像数据本地存储，不上传云端 |
| **可移植** | 跨平台（Windows/Linux/macOS），零外部服务依赖 |
